## Deep dive into DevOps

### 1. Continuous Integration[CI]:
- Every time a developer pushes code-->tests run automatically and if something breaks -->the pipeline stops, until we fix it before then move forward.

### 2. Continuous Deployment[CD]:
- If the tests pass--> the update is automatically deployed to staging or production ..
- So no late night deployments manually